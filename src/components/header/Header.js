import UpperMenu from './UpperMenu';
import Logo from './Logo';

function Header() {
  return (
    <header>
      <UpperMenu />
      <Logo />
    </header>
  );
}
export default Header;
